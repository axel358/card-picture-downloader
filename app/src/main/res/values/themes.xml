<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.CardDownloader" parent="Theme.Material3.Dark">
        <item name="toolbarStyle">?attr/toolbarSurfaceStyle</item>
        <item name="android:statusBarColor">?attr/colorSurface</item>
        <item name="android:navigationBarColor">?attr/colorSurface</item>
    </style>

    <style name="Theme.CardDownloader" parent="Base.Theme.CardDownloader" />

    <style name="Theme.CardDownloader.NoActionBar" parent="Theme.CardDownloader">
        <item name="windowNoTitle">true</item>
    </style>
</resources>